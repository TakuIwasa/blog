version: 0.2

env:
  parameter-store:
    hitokotomono-db: 'HITOKOTOMONO-DB'
    hitokotomono-host: 'HITOKOTOMONO-HOST'

phases:
  build:
    commands:
      - echo db:${HITOKOTOMONO-DB}
      - echo host:${HITOKOTOMONO-HOST}